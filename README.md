# Builds fork:
https://github.com/ahostwin/firecrackmanager
## Upstream:
https://github.com/dtouzeau/firecrackmanager

# This is a simple way to build and package using docker
- It clones the latest commit, builds the static binary and debian package
## Requires docker and make
## Clone and run:
```bash
make
```
